<% subscription = Subscription.find_by(project: project, user: current_user) %>
<%= form_with model: [current_user, subscription], local: true, method: :delete  do |f| %>
  <%= f.submit "Delete Project Participation" %>
<% end %>
